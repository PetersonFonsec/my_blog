import SectionProfile from "./index";

export default {
  component: SectionProfile,
  title: "SectionProfile",
};

const Template = (args) => <SectionProfile {...args} />;

export const Default = Template.bind({});
