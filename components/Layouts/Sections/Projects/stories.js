import SectionProjects from "./index";

export default {
  component: SectionProjects,
  title: "SectionProjects",
};

const Template = (args) => <SectionProjects {...args} />;

export const Default = Template.bind({});
