import SectionSkills from "./index";

export default {
  component: SectionSkills,
  title: "SectionSkills",
};

const Template = (args) => <SectionSkills {...args} />;

export const Default = Template.bind({});
